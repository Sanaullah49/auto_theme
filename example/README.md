# auto_theme_example

Example application for the `auto_theme` package.

This app demonstrates:

- generating the opposite theme from a single source theme
- switching between light, dark, and system modes
- theming common Material widgets like cards, inputs, dialogs, and navigation

Run it with:

```bash
flutter pub get
flutter run
```
