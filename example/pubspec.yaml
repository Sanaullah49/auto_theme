name: auto_theme_example
publish_to: none
version: 0.1.0+1
description: Example app for the auto_theme package.

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  auto_theme:
    path: ..

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
